{
  "name": "habit-timer-cloudflare-companion",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:local": "wrangler d1 migrations apply habit-timer-companion --local",
    "db:remote": "wrangler d1 migrations apply habit-timer-companion --remote",
    "sync:library": "node scripts/repair-library.mjs",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.8.3",
    "wrangler": "^4.24.3"
  },
  "dependencies": {
    "lucide-static": "^1.41.0"
  }
}
